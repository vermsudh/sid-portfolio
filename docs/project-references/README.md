# Project Reference Docs

Archival source material for individual projects — **not read by the application at
runtime** (nothing in `app/` or `components/` imports from this folder). These exist
so the short, factual copy on the site and case-study pages can be written/verified
against fuller internal documentation.

## Expected files (to be added by Sid)

These were referenced in the redesign spec (Stage 12) but were **not provided** during
the redesign build, so they are not yet in the repo. Drop them here when available:

- `Global_Lutyens_Case_Study.md` — source for `content/projects/global-lutyens.ts`
  (its `fullCaseStudy` field is currently a `pending-source-doc` placeholder).
- `CAPTCHA_Solving_Agent.md` — source for `content/projects/captcha-solving-agent.ts`.
- `Wee_Portfolio_Spec.md` (or the master projects reference) — source for
  `content/projects/wee-portfolio.ts`.

> Placeholder only — remove this note once the real reference docs are added.
